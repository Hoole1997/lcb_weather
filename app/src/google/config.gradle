ext {
    // AdMob配置
    admob = [applicationId: "ca-app-pub-7869168547621070~4367463915", // 测试应用ID，请替换为实际的AdMob应用ID
             adUnitIds    : [banner      : "",        // 横幅广告测试ID
                             interstitial: "ca-app-pub-7869168547621070/5591098986", // 插页广告测试ID
                             splash      : "ca-app-pub-7869168547621070/6904180656",       // 开屏广告测试ID
                             native      : "ca-app-pub-7869168547621070/3212347658",        // 原生广告测试ID
                             full_native : "ca-app-pub-7869168547621070/3212347658",        // 全屏原生广告测试ID
                             rewarded    : ""         // 激励广告测试ID
             ]]

    // Google Ad Manager 配置（按业务替换为正式 ID）
    gam = [adUnitIds: [banner      : "",
                       interstitial: "/23312177113/DailyWeather_IV",
                       splash      : "",
                       native      : "",
                       full_native : "",
                       rewarded    : ""
    ]]

    // Pangle配置
    pangle = [applicationId: "8826816", // Pangle测试应用ID
              adUnitIds    : [splash      : "890156393",        // 开屏广告测试ID（竖屏）
                              banner      : "",        // 横幅广告测试ID（320x50）
                              interstitial: "983202263",       // 插页广告测试ID（竖屏）
                              native      : "983202265",        // 原生广告测试ID
                              full_native : "983202265",        // 全屏原生广告测试ID
                              rewarded    : ""         // 激励视频测试ID（竖屏）
              ]]

    // TopOn配置
    topon = [applicationId: "h1hbtmvc78rfjs",           // TopOn 应用 ID（需替换为实际值）
             appKey       : "a245cf5571ee648c147fecabf477307b7",          // TopOn 应用密钥（需替换为实际值）
             adUnitIds    : [interstitial: "n1hbtmvc9q6nch",  // 插页广告位 ID（需替换为实际值）
                             rewarded    : "",  // 激励广告位 ID
                             native      : "n1hbtmvcb7qd8i",  // 原生广告位 ID（需替换为实际值）
                             splash      : "n1hbtmvcboree3",  // 开屏广告位 ID（需替换为实际值）
                             full_native : "n1hbtmvcb7qd8i",  // 全屏原生广告位 ID（需替换为实际值）
                             banner      : ""]  // 横幅广告位 ID（需替换为实际值）
    ]

    // MAX (AppLovin) 配置
    max = [sdkKey   : "",  // Demo SDK Key（测试用）
           adUnitIds: [splash      : "",      // 开屏广告位 ID
                       banner      : "",      // 横幅广告位 ID
                       interstitial: "",// 插页广告位 ID
                       native      : "",      // 原生广告位 ID
                       fullNative  : "", // 全屏原生广告位 ID
                       rewarded    : ""]    // 激励广告位 ID
    ]

    // 应用版本配置
    app = [applicationId: "com.local.weather.daily.tool",
           compileSdk   : 36,
           minSdk       : 26,
           targetSdk    : 36,
           versionCode  : 4,
           versionName  : "1.0.3"]

    // 统计归因配置
    analytics = [adjustAppToken: "",           // Adjust App Token
                 thinkingDataAppId: "",   // 数数 SDK APP ID
                 thinkingDataServerUrl: "",     // 数数上报域名
                 defaultUserChannel: "natural"]                           // 默认用户渠道，internal默认paid
}
